# Example .env configuration for E2B Custom Template
# Copy this section to your .env file and fill in the values

# After building your custom template, add the template ID here:
E2B_TEMPLATE_ID=flowai-langgraph-abc123xyz

# Note: If E2B_TEMPLATE_ID is not set or empty, the system will:
# 1. Use default E2B sandbox
# 2. Install dependencies on every execution (~30 second overhead)
#
# With custom template configured:
# 1. Use pre-configured sandbox
# 2. Dependencies already installed (~5 second startup)
